# Copy this to .env and fill in values. Never commit .env.

# MongoDB (dev)
MONGO_URL=mongodb://localhost:27017
MONGO_DB_NAME=transcelerate

# MongoDB (test) — used by pytest via docker compose --profile test
MONGO_TEST_URL=mongodb://localhost:27018
MONGO_TEST_DB_NAME=transcelerate_test

# CDISC Library API — optional; free developer key does not unlock members-only COSMoS data
CDISC_API_KEY=your_primary_key_here
CDISC_BASE_URL=https://api.library.cdisc.org/api

# Gate 3A reads a pinned local snapshot from https://github.com/cdisc-org/COSMoS
COSMOS_DATA_PATH=app/data/cosmos
COSMOS_SOURCE_COMMIT=record_the_pinned_commit_here

# REDCap project — required for Gate 3B live verification, not Gate 3A offline export
# STATUS: pending JHU ICTR response (redcap@jhu.edu)
REDCAP_URL=https://your-redcap-instance.org
REDCAP_API_TOKEN=your_token_here
REDCAP_PROJECT_ID=your_project_id

# FastAPI
API_HOST=0.0.0.0
API_PORT=8000
DEBUG=true
